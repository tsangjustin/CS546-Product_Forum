(function($) {
    var forumsList = $('#community-forum-list');
    var forumFilter = $('#forums-filter');
    var communityForums = [];

    // console.log(forumsList.find('a').each(function(idx, val) {
    //     communityForums.push({
    //         createdOn:
    //         text: val.text
    //     })
    //     console.log(idx);
    //     console.log(val.text);
    // }))

    forumFilter.change(function(e) {
        switch (e.target.value) {

        }
    });
})(jQuery)
